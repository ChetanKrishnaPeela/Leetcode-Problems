class Solution {
    public int minAddToMakeValid(String s) {
        int n=s.length(), l=0, r=0;
        for(int i=0; i<n; i++)
        {
            if(s.charAt(i)=='(') l++;
            else if(l > 0) l--;
            else r++;
        }
        return l+r;
    }
}
